{
  "name": "instrument",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:showcase": "bun --filter @instrument/showcase dev",
    "lint:showcase": "bun --filter @instrument/showcase lint"
  }
}